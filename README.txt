LSC Web AI provides a simple and intuitive administration interface through a web browser.

You can handle the following LSC operations :
- start/stop the LSC batch instance (one time synchronization)
- start/stop a LSC service instance (event-based sunchronization)
- launch a synchronous task on a dedicated object id
- configure tasks, connections, services and synchronization options
- consult logs
